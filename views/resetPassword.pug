doctype html
html(lang="en")
    head
        include partials/header
    body.login 
        .contenedor-formulario
            h1= nombrePagina 
            p Coloca tu nueva contraseña
           
                                
            form.caja-login(method="POST")
                .campo 
                    label(for="password") Nueva contraseña: 
                    input(type="password" name="password" placeholder="Coloca tu nueva contraseña" required value= email)
                .campo.enviar
                    input.boton(type="submit" value="Restablecer contraseña")
                .campo.acciones

